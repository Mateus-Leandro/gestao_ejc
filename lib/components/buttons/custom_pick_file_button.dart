import 'package:flutter/material.dart';

class CustomPickFileButton extends IconButton {
  CustomPickFileButton({
    required super.onPressed,
    required super.icon,
    super.iconSize = 180
  });

}
